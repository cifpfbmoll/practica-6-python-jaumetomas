#P6E7
#Escribe un programa que pida un número (límite) y luego te pida números hasta que la suma de 
#los números introducidos supere el límite inicial. El programa termina escribiendo la lista de números.
lista= []
valores =0
limite= int(input("Escribe un numero limite "))
valores= int(input("Escribe valores "))
lista.append (valores)
while (valores <= limite):
    valores += int(input("Escribe valores "))
    lista.append (valores)
if (valores > limite):
    lista.remove(valores)
    print ("El limite es" ,limite, " por lo que la lista es" ,lista, "y el resultado es" ,valores,)

    
